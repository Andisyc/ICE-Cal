# FADA governance receipts

This directory preserves dated governance receipts. The newest filename is not automatically the
current authority; use `../contracts/README.md` and `../../governance/README.md` first.

Current Source Oracle receipt:

- `2026-09-07-v024-command-gated-phase-contact-design-confirmed.json`

The v022 and earlier receipts are retained as historical provenance. Their internal `current` fields
describe the transaction at the time they were issued and do not override the v024 Contract.

The repository-simplification execution receipt is an engineering-maintainability record. It does
not authorize or validate v024 method implementation.
