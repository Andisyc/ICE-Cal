# Current Impact Rules

Any change to axis meaning, analytic target, Direction Bank shape/normalization, coefficient input or
output, stage loss/ownership, scale mapping, composition order, event fallback, schema, or first-action
consumer impacts v007/v006 semantics and requires Module Test Card review.

Changes to H/K/D, Tracker checkpoint identity, stage-owned config, predecessor-path signatures,
`direction_frozen` / `coefficient_frozen` owner sets, fixed gate thresholds, canonical Scale Evidence
scan grid, parent/evidence byte digest semantics, deployment entrypoint, effective fault grid, or curve
artifact invalidate downstream module/formal evidence. Publication changes must re-prove unique-temp
cleanup and preservation of a preexisting target.

The content identity covers all 21 active semantic-surface files, including unchanged owners reused by
the 17-row manifest. A narrower implementation change list is review scope only and cannot replace the
active-surface identity. Historical v006/v005 tests and `CONNECTIVITY-ONLY` sentinels cannot close
current semantic or formal impacts.
