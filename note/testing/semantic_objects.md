# UniLab Semantic Objects

Semantic objects are values whose meaning crosses module boundaries. Search by all aliases before changing one.

| Object | Aliases / fields | Owners | Persistence/play risk | Required S/T |
| --- | --- | --- | --- | --- |
| G1 actor observation layout | `obs`, `obs_groups_spec`, actor obs, `mode_observation`, `observe_height_command`, command dim | `G1WalkEnv`, `train_offpolicy.py`, `play_interactive.py`, `interactive_playback.py` | High: checkpoint actor input dim must match playback env obs | S1/S2/S3; T-shape, T-order, T-persist |
| G1 command distribution | `commands`, `vel_limit`, `rel_standing_envs`, `rel_transition_envs`, `small_xy_threshold`, `gait_enabled` | `Commands`, `sample_g1_walk_commands`, DR provider | Medium: run_config restores command profile; live distribution affects policy | S1/S2/S4; T-dist, T-role, T-live |
| G1 height command | `height_commands`, `commands_height`, `height_range`, `default_height`, `observe_height_command`, `min_base_height` | `Commands`, G1 height config, `_height_command_column`, reward config | High: adds actor obs dimension and can conflict with survival height | S1/S2/S3; T-shape, T-order, T-persist, T-scale, T-oracle |
| G1 height reward scale | `track_base_height_exp_smooth`, `tracking_sigma`, `tracking_lin_vel`, `feet_phase`, `alive` | G1 height config, reward dispatch, common reward functions | Medium: can bias policy toward crouching or no-motion if scale/range dominates walking rewards | S1/S2/S4; T-value, T-meta, T-scale, T-live |
| G1 stand-still expert | `G1StandStill`, `g1_stand_still`, zero command, stand-only reward scales, no mode/height route, both-feet contact, contact balance, contacted-foot slide | G1 stand-still task config, `G1WalkEnv`, reward dispatch, symmetry layout, MuJoCo foot sensors | High: must not inherit Walking/height/mixed-mode keys, checkpoint actor input must stay 98-dim, and static support terms must not penalize required nonzero support action | S0/S1/S2/S3/S4; T-connect, T-oracle, T-value, T-shape, T-persist, T-live |
| G1 stand high-support height bundle | `stand_support_height_exp`, `stand_support_height_margin_l2`, `stand_base_height_deficit_l1`, support-relative height, terrain/base height, contacted feet height, high loaded stand, low crouch, 0.677m low equilibrium, 0.709m low equilibrium | G1 stand-still task config, G1 standing reward owner, foot sensors, reward dispatch, live sentinel logs | High: must reject low crouch without leaking into `G1WalkFlat`, must penalize visible base-height deficits even when support-relative geometry is plausible, and must not be gameable by missing contact, foot sliding, or floating height | S1/S2/S4; T-value, T-role, T-diff, T-oracle, T-scale, T-live |
| G1 reward mode | `reward.mode`, `standing_enabled`, `stand_terms`, `walk_terms`, `stand_recovery_terms`, overrides | `G1RewardConfig`, `_compute_mode_reward` | Medium: run_config restores reward_config in playback; training distribution sensitive | S1/S2/S4; T-role, T-diff, T-live |
| G1 gait phase | `gait_phase`, `stand_phase`, `feet_phase`, `feet_phase_contrast`, `feet_phase_contact` | gait helpers, DR provider, `apply_action` | Medium: observation and reward share the same phase | S1/S2/S4; T-value, T-transform, T-live |
| G1 action execution | `current_actions`, `executed_actions`, `stand_action_authority`, `action_scale`, `default_angles`, PD target | `G1WalkEnv.apply_action` | High: direct-fall bug may live here | S1/S2/S4; T-transform, T-scale, T-live |
| G1 reset physical state | qpos, qvel, `reset_base_qvel_limit`, `standing_reset_base_qvel_limit`, base height, tilt | DR provider, backend reset, env update_state | High: direct-fall bug may live here | S2/S4; T-oracle, T-live |
| Checkpoint contract | `run_config.json`, actor state dict, actor input dim, obs_normalizer, `algo.load_run`, `algo.checkpoint` | training tracker, play scripts, visualization playback | High: old/new checkpoint compatibility | S3/S4; T-persist, T-order, T-diff, T-live |
| DAgger persistent collection request | `DaggerCollectRequest`, `DaggerCollectResult`, `request_id`, `scenario`, `iteration`, `checkpoint_path`, `output_path`, `expected_weight_version`, `observed_weight_version`, `num_samples`, `worker_pid`, structured metrics, `PersistentResourceIdentity`, lifecycle close report | `src/unilab/algos/torch/distill/{async_runtime,persistent_runtime,persistent_resources,g1_persistent_worker}.py`, UniLab `AsyncRunner`, `SharedWeightSync`, workflow adapter | High: every scenario in one outer iteration must consume one checkpoint/version; teacher/env reuse requires complete owner identity; each request resets once; identity mismatch, worker failure, and cleanup fail closed without copying runner/IPC code into distill | S1/S2/S3/S4; T-connect, T-order, T-persist, T-oracle, T-live |
| DAgger performance metric identity | `DistillationStageObservation`, `DistillationStageObservationAccumulator`, `performance_clock`, `DistillationMetricIdentity`, `DistillationStageMetric`, execution mode, outer iteration, scenario, request/PID, checkpoint/hash/version, teacher hashes, config hash, seed/device/num_envs, stage seconds, inference rows, packed rows, env steps, rows/second, success/error/cleanup | `src/unilab/algos/torch/distill/performance.py`; role/transition collectors; persistent G1 worker; future workflow connector | High: collector/worker observations remain identity-free until parent lineage; OFF default emits no metadata; inference counts represent actual forwards, not selected rows; integration is partial and cannot support a speedup claim | S1/S2/S3/S4; T-connect, T-order, T-persist, T-oracle, T-diff, T-scale, T-live |
| Behavior distillation target | `student_obs`, `teacher_obs`, `teacher_action`, `privileged_actions`, detached target, `behavior_loss`, `aux_loss`, `aux_loss_coef`, `route_entropy`, `student_state_dict`, `LoadedDistillationStudentPolicy`, `create_distill_playback_session`, `--algo distill`, `--task`, `task_owner`, `algo_log_name=distill`, `DistillationTensorDataset`, `dataset_path`, `dataset_metadata`, `distill_source`, `dataset_num_samples`, `samples_seen`, `agent_steps`, `teacher_metadata`, `checkpoint_actor_input_dim`, `checkpoint_first_weight_key`, `teacher.obs_dim=99`, legacy `teacher.obs_dim=100`, stand-still `teacher.obs_dim=98`, `teacher.checkpoint_path`, `teacher.load_run`, `teacher.checkpoint`, `teacher_policy`, `teacher_policy_checkpoint_path`, `student.model_type`, `student_model_type`, `student.obs_dim=99`, stand-still `student.obs_dim=98`, `student_action_dim`, `student_hidden_dims`, `student.num_experts`, `student_num_experts`, `student_expert_hidden_dims`, `student_router_hidden_dims`, `student.routing_mode`, `student_routing_mode`, `student.router_temperature`, `student_router_temperature`, `MoEStudentPolicy`, `MoEStudentOutput`, `router_logits`, `route_probs`, `expert_actions`, `expert_usage`, `selected_expert`, `checkpoint_path`, `actions_abs_max`, `policy_action_nonzero`, `training.dry_run`, `training.dry_run_updates`, `training.dry_run_batch_size`, `training.offline_dataset_path`, `training.offline_batch_size`, `training.offline_max_updates`, `training.offline_checkpoint`, `training.formal_run`, `training.formal_run_name`, `training.formal_run_dir`, `run_config_path`, `run_summary_path`, `model_<samples_seen>.pt`, `training.collect_dataset_path`, `training.collect_num_samples`, `training.collect_num_envs`, `training.collect_action_mode`, `training.collect_action_seed`, `action_abs_max`, `training.collect_teacher_obs_key`, `training.collect_teacher_projection`, `training.collect_student_projection`, `training.collect_student_drop_index`, `synthetic_teacher_tail`, `--temp-student-model-type` | `conf/distill`, `scripts/train_distill.py`, `scripts/play_interactive.py`, `scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py`, `scripts/deploy/check_unilab_g1_distill_teacher_obs_contract.py`, `src/unilab/algos/torch/distill`, `src/unilab/visualization/interactive_playback.py` | High: teacher/privileged observations must not leak into deployable playback, teacher gradients must remain absent, default teacher checkpoint route is 99-D to match current live `G1WalkHeight` actor obs, explicit `teacher.checkpoint_path` is the direct checkpoint-file route for rollout collection and bypasses `teacher.load_run` / `teacher.checkpoint` resolution, 100-D teacher checkpoints require explicit `teacher.obs_dim=100` plus `training.collect_teacher_projection=pad_zeros`, checkpoint actor input dim is preflighted before trainer construction, deployable height student actor route stays 99-D, explicit `task=g1_stand_still/mujoco` owns the accepted 98-D standing teacher/student route, the teacher-obs audit entrypoint must receive that owner route through `--task` before judging the standing checkpoint, and `training.collect_action_mode=teacher_policy` is guarded to explicit 98-D `G1WalkFlat`/`G1StandStill` routes while `G1WalkHeight` remains rejected; `student.model_type=mlp` is the default OFF path, `student.model_type=moe` is fake-batch configurable, student-only playback-loadable from checkpoint metadata, live-confirmed for temp-checkpoint one-step MuJoCo paths, default 99-D Hydra dry-run and saved-dataset MoE offline update are runtime-confirmed through `scripts/train_distill.py` and reload with `checkpoint_actor_input_dim=99`, live reset-observation dataset collection is runtime-confirmed from `training.collect_dataset_path`, random live collection is runtime-confirmed as a nonzero action-source connectivity path with `action_abs_max>0`, stand-still and walk-flat teacher-policy collection are runtime-confirmed as nonzero 98-D/98-D action-source paths with `teacher_policy_checkpoint_path`, `action_abs_max>0`, `env_steps>0`, and bounded saved-dataset offline update/reload, stand-still trained-student live playback is runtime-confirmed for reset/step connectivity with 98-D owner config and nonzero 29-D policy action, stand-still formal saved-dataset lifecycle is runtime-confirmed for `run_config.json`, `run_summary.json`, `model_<samples_seen>.pt`, checkpoint reload, and live playback of the formal checkpoint, and the formal checkpoint now reaches the actual `mjpython` MuJoCo viewer launch boundary; default live collection saves 99-D teacher obs with `synthetic_teacher_tail=False`, while the explicit legacy bridge pads teacher obs to 100-D with `synthetic_teacher_tail=True`, proving storage/connectivity but not real teacher privileged semantics; replay storage, online/replay training, trained MoE policy quality, real height teacher quality, human visual quality, and expert role semantics are still unconfirmed; teacher/student/checkpoint dimensions must fail explicitly on mismatch | S1/S2/S3/S4; T-grad, T-detach, T-shape, T-role, T-persist, T-live |
| Behavior distillation MoE expert semantics | `diagnose_moe_expert_routes`, `moe_diagnostics_to_dict`, `MoEExpertDiagnostics`, `MoERoleRouteSummary`, `role_labels`, `expert_fraction`, `mean_entropy`, `dominant_expert`, `collapse_detected`, `collapse_fraction`, `hard_routing` | `src/unilab/algos/torch/distill/moe_diagnostics.py`, `tests/algos/test_g1_distillation_contract.py`, `scripts/deploy/check_unilab_g1_distill_moe_expert_semantics.py` | High: toy role fixtures prove expert diagnostics are readable by role and collapse guard works; real `G1StandStill` collected data currently has no role labels or command/height/velocity diversity, so it can only confirm overall usage, entropy, and non-collapse, not real semantic role separation | S1/S3/S4; T-role, T-shape, T-persist, T-live |
| Behavior distillation role-labelled dataset | `DistillationTensorDataset.role_labels`, metadata `role_labels`, payload `role_labels`, `DistillationBatch.role_labels`, `stand`, `walk_height`, `stand_height`, role-labelled dataset | `src/unilab/algos/torch/distill/data.py`, `src/unilab/algos/torch/distill/trainer.py`, `scripts/deploy/check_unilab_g1_distill_moe_expert_semantics.py`, `tests/algos/test_g1_distillation_contract.py` | High: MoE cannot learn or diagnose standing/walking/height conflict as semantic roles if samples lose role identity during save/load/batch slicing; OFF path keeps old datasets with no labels as `None`, ON path persists and validates role labels before diagnostics | S1/S3; T-role, T-shape, T-persist |
| Behavior distillation cached target | `DistillationTensorDataset.teacher_actions`, payload `teacher_actions`, `teacher_action_dim`, `DistillationBatch.teacher_actions`, cached teacher action, cached target, `teacher_action_source`, `last_teacher_action_source` | `src/unilab/algos/torch/distill/data.py`, `src/unilab/algos/torch/distill/trainer.py`, `src/unilab/algos/torch/distill/offline.py`, `scripts/train_distill.py`, `tests/algos/test_g1_distillation_contract.py` | High: multi-teacher MoE data cannot assume all teachers share one `teacher_obs` layout; cached 29-D action targets let offline updates train from heterogeneous teacher sources at the shared action boundary, while old datasets without cached targets still use the frozen-teacher path | S1/S3; T-grad, T-detach, T-shape, T-persist |
| Behavior distillation role-conditioned MoE loss | `algo.role_loss_coef`, `algo.role_expert_targets`, `role_loss`, `role_target_count`, `last_role_loss`, `last_role_target_count`, router logits, role-conditioned router loss | `conf/distill/config.yaml`, `src/unilab/algos/torch/distill/trainer.py`, `src/unilab/algos/torch/distill/offline.py`, `scripts/train_distill.py`, `tests/algos/test_g1_distillation_contract.py`, `tests/config/test_config_system.py`, `tests/scripts/test_train_scripts.py` | High: role labels must not become deploy-time inputs, but when explicitly enabled they supervise MoE router logits through a fail-closed mapping from role label to expert index; OFF default keeps old behavior unchanged, while ON requires explicit mapping and role labels | S1/S2/S3; T-grad, T-role, T-shape, T-persist |
| Behavior distillation command intent routing | `command_intent`, `command_intents`, `commands`, `command_active_mask`, `collect_command_sample_filter`, `collect_command_info_key`, `collect_command_xy_threshold`, `collect_command_yaw_threshold`, `command_intent_loss_coef`, `command_intent_expert_targets`, `command_intent_loss`, `command_intent_target_count`, `role_labels`, `walk_flat`, `stand`, `role_expert_targets`, `offline_balance_key`, `offline_balanced_labels`, `offline_batch_label_counts`, `last_balance_label_counts`, `batch_label_counts`, `router_logits`, `route_probs`, `selected_expert`, `expected_intent`, `expected_expert`, playback trace command | `src/unilab/algos/torch/distill/collector.py`, `src/unilab/algos/torch/distill/data.py`, `src/unilab/algos/torch/distill/trainer.py`, `src/unilab/algos/torch/distill/moe_student.py`, `src/unilab/algos/torch/distill/offline.py`, `scripts/train_distill.py`, `scripts/play_interactive.py`, `src/unilab/visualization/interactive_playback.py`, `conf/distill/config.yaml`, `conf/distill/task/g1_walk_flat/mujoco.yaml`, `conf/distill/task/g1_stand_still/mujoco.yaml` | High: collection filters already separate active walking samples from inactive standing samples, CR-4 makes command intent an explicit offline MoE router training signal, CR-5 enforces the deploy-time command route, and CR-6 adds an explicit offline balanced sampler so stand/walk or inactive/active batch proportions are not left to dataset order or random shuffle. Fused MoE deployment must still prove trained behavior in MuJoCo; height control is future work and must add an explicit third intent instead of silently mixing 99-D height data into the 98-D walk/stand route | S1/S2/S3/S4; T-mask, T-role, T-dist, T-grad, T-persist, T-live |
| Behavior distillation learner runtime identity | offline update number, recent batch ring buffer, `builtins.int`, `target_indices`, `target_indices.append`, raw/converted expert target, `sys.gettrace`, `sys.getprofile`, trainer stage, serialization callable | `src/unilab/algos/torch/distill/{offline,trainer,data}.py`, `scripts/train_distill.py`, `tests/algos/test_g1_distillation_contract.py` | High: repeated formal runs failed at different long-lived parent-process boundaries with callable/type anomalies. Owner-local structured snapshots must identify the first changed object without changing loss, sampling, optimizer, checkpoint, or workflow semantics | S1/S2/S4; T-transform, T-oracle, T-state, T-update-order, T-live |
| Behavior distillation multi-task dataset adapter | `training.multitask_dataset_path`, `training.multitask_sources`, `training.multitask_expected_student_obs_dim`, `training.multitask_expected_teacher_obs_dim`, `training.multitask_expected_teacher_action_dim`, `build_multitask_distillation_dataset`, `multitask_adapter`, `source_roles`, `source_paths`, `source_sample_counts`, saved source `{path, role}` | `conf/distill/config.yaml`, `src/unilab/algos/torch/distill/data.py`, `scripts/train_distill.py`, `tests/algos/test_g1_distillation_contract.py`, `tests/scripts/test_train_scripts.py`, `tests/config/test_config_system.py` | High: multi-task MoE training needs a single role-labelled cached-target dataset, but source tasks may have incompatible obs layouts; adapter infers one common source dim by default so 98-D standing/walking data is not rejected by the default 99-D height config, and optional `multitask_expected_*_dim` fields provide explicit fail-closed checks when desired; adapter requires cached `teacher_actions` and does not silently project stand/walk/height data across owner routes | S1/S2/S3; T-shape, T-role, T-persist |
| Behavior distillation multi-task MoE runtime probe | `check_unilab_g1_distill_multitask_runtime_probe.py`, `g1_distill_multitask_runtime`, `role_counts`, `teacher_action_source=cached`, `last_role_loss`, `last_role_target_count`, `last_student_grad_norm`, raising teacher sentinel | `scripts/deploy/check_unilab_g1_distill_multitask_runtime_probe.py`, `scripts/train_distill.py`, `src/unilab/algos/torch/distill/data.py`, `src/unilab/algos/torch/distill/trainer.py`, `src/unilab/algos/torch/distill/offline.py`, `tests/scripts/test_train_scripts.py` | High: proves the saved-source multi-task adapter, cached target path, and role-conditioned MoE loss can run together in one bounded offline update without calling a teacher module; it is a toy tensor runtime proof and does not claim MuJoCo dynamics, real teacher quality, or trained policy behavior | S2/S3; T-connect, T-grad, T-shape, T-role, T-persist |
| Behavior distillation dual-teacher walking/standing source | `g1_walk_flat/mujoco`, `G1WalkFlat`, `check_unilab_g1_distill_dual_teacher_moe_probe.py`, `g1_distill_dual_teacher_moe`, `walk_flat`, `stand`, walking checkpoint `2026-07-09_02-48-58_mujoco/model_5000.pt`, standing checkpoint `2026-07-09_22-55-05_mujoco/model_5000.pt`, cached 29-D `teacher_actions` from `teacher_policy` collection | `conf/distill/task/g1_walk_flat/mujoco.yaml`, `conf/distill/task/g1_stand_still/mujoco.yaml`, `scripts/train_distill.py`, `src/unilab/algos/torch/distill/collector.py`, `scripts/deploy/check_unilab_g1_distill_dual_teacher_moe_probe.py`, `tests/config/test_config_system.py`, `tests/scripts/test_train_scripts.py`, `tests/algos/test_g1_distillation_contract.py` | High: real walking and standing teachers are both 98-D and can be sampled through their own owner routes into cached action targets for one merged MoE update; `G1WalkHeight` remains a separate 99-D route and must not be silently mixed with flat walking or standing without explicit projection/design | S2/S3/S4; T-live, T-connect, T-grad, T-shape, T-role, T-persist |
| Behavior distillation viewer path | `check_unilab_g1_distill_viewer_path.py`, `viewer_command`, `mjpython`, `viewer_model_nq`, `viewer_model_nv`, `viewer_model_nu`, `state_transfer`, `qpos_shape`, `qvel_shape`, `ctrl_shape`, `_load_viewer_model`, `mujoco.MjData`, `mj_setState`, `mj_forward`, `mujoco.viewer.launch_passive`, `Opening viewer`, `Controls` | `scripts/play_interactive.py`, `scripts/deploy/check_unilab_g1_distill_viewer_path.py`, `src/unilab/visualization/interactive_playback.py` | High: the scriptable preflight proves the formal 98-D stand-still checkpoint reaches the `play_interactive.py --algo distill` session route, loads the MuJoCo viewer model, and transfers finite physics state into viewer data; the actual `mjpython` GUI launch path was held alive for 15 seconds and reached the viewer opening/control logs, but human visual quality and long-horizon behavior remain qualitative/unclaimed | S4; T-live, T-connect, T-shape, T-persist |
| Replay/storage tuple | obs, actions, rewards, dones, next obs, info/log, stats | IPC and offpolicy runner | High when storage fields change | S1/S2/S3; T-shape, T-order, T-mask, T-persist |
| Normalizer / obs stats | `obs_normalization`, `obs_normalizer`, shared obs stats, mean/std | algorithms, IPC, checkpoint/play | High for export/play/eval | S1/S2/S3; T-shape, T-persist, T-diff |

## Search Rule

When debugging a semantic object, search aliases across:

```text
conf/
scripts/
src/unilab/
tests/
note/architecture/
note/testing/
```

Then assign required S/T before editing.
