# UniLab Test Inventory

This file maps test commands to S tiers and T kinds. It is intentionally module-oriented, not just a list of pytest files.

## Core Commands

| Command | S tiers | T kinds | Main modules | Evidence class |
| --- | --- | --- | --- | --- |
| `uv run pytest tests/test_cli.py -q` | S0, S2 | T-connect, T-oracle | U-E-01, G1LOC-E-002, G1LOC-C-004; includes `--task g1_stand_still` -> `task=sac/g1_stand_still/mujoco` CLI route | contract-confirmed |
| `uv run pytest tests/scripts/test_train_scripts.py -q` | S0, S2, S3 | T-connect, T-oracle, T-persist, T-diff | U-E-02, U-C-01, U-C-02, G1LOC-T-001, G1LOC-T-002, G1LOC-P-001, G1LOC-P-002; includes generic `scripts/train_distill.py` assembly, MLP and MoE student selection, stand-still 98-D owner assembly, fake-batch dataset probe, teacher checkpoint input-dim preflight, explicit `teacher.checkpoint_path` resolver bypass for direct checkpoint-file collection commands, stand-still 98-D teacher obs preflight, saved-dataset offline update, live-env dataset collection fake-env contract, stand-still 98/98 collection owner contract, zero/random collection action-source contract, explicit 98-D G1WalkFlat/G1StandStill teacher-policy collection guard, cross-owner G1WalkFlat+G1StandStill student-policy DAgger inactive command-distribution override, bounded stand-still teacher-policy saved-dataset offline update, formal stand-still run metadata/checkpoint contract, teacher obs contract audit fake-env contract, bounded offline updates, aux/route diagnostics, student checkpoint output, MLP/MoE student-only checkpoint load metadata, default 99-D and stand-still 98-D distill playback sentinel contracts, and sentinel temp MoE checkpoint creation | contract-confirmed |
| `uv run pytest tests/config/test_config_system.py -q -k distill` | S0, S2 | T-connect, T-oracle, T-shape | U-C-01, U-C-02, U-A-01 generic distill config root; confirms `conf/distill` and `task=g1_walk_height/mujoco` compose with default teacher 99->29, student 99->29, height-command observation enabled, default `student.model_type=mlp`, inert `algo.aux_loss_coef=0.0`, inert saved-dataset offline defaults, inert formal-run defaults including `formal_run=false`, inert live-collection defaults including `collect_action_mode=zero` and `collect_action_seed=null`; also confirms explicit `task=g1_walk_flat/mujoco` and `task=g1_stand_still/mujoco` owners compose with no height-command observation, selected `model_5000.pt` runs, and 98-D teacher/student dims | contract-confirmed; live collection and formal run have separate sentinels; no policy-quality claim |
| `uv run pytest tests/scripts/test_visualization_entrypoints.py -q -k distill` | S0, S2 | T-connect, T-oracle | U-X-01 generic distill interactive entrypoint; confirms `--algo distill` composes `conf/distill`, builds playback args, and routes to `create_distill_playback_session` without launching viewer | contract-confirmed; no live viewer or MuJoCo reset/step claim |
| `uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --steps 1 --action-mode zero --device cpu` | S4 | T-live, T-connect, T-shape | U-C-01, U-X-01, U-A-01 generic distill live playback; confirms `conf/distill` creates a real `G1WalkHeight` MuJoCo env and `create_distill_playback_session` reaches reset/step with actor obs, 29-dim actions, and finite physics state | runtime-confirmed for zero-action lifecycle only; no viewer window, policy checkpoint quality, or training claim |
| `uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --steps 1 --action-mode policy --make-temp-policy-checkpoint --device cpu` | S4 | T-live, T-connect, T-shape, T-persist | U-C-01, U-X-01, U-A-01 generic distill live playback; confirms a temporary MLP student checkpoint loads with 99-D actor obs, emits nonzero 29-D actions, and steps the real `G1WalkHeight` MuJoCo env | runtime-confirmed for temp MLP checkpoint policy path only; no viewer window, trained policy quality, replay/storage, MoE live rollout, or formal training claim |
| `uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --steps 1 --action-mode policy --make-temp-policy-checkpoint --temp-student-model-type moe --device cpu` | S4 | T-live, T-connect, T-shape, T-persist | U-C-01, U-X-01, U-A-01 generic distill live playback; confirms a temporary MoE student checkpoint loads with 99-D actor obs, emits nonzero 29-D actions, and steps the real `G1WalkHeight` MuJoCo env | runtime-confirmed for temp MoE checkpoint policy path only; no viewer window, trained policy quality, replay/storage, or formal training claim |
| `uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --steps 1 --action-mode policy --load-run <distill_student.pt> --device cpu` | S4 | T-live, T-connect, T-shape, T-persist | U-C-01, U-X-01, U-A-01 generic distill live playback; confirms a saved MoE student checkpoint produced by live-dataset offline update loads with 99-D actor obs, emits nonzero 29-D actions, and steps the real `G1WalkHeight` MuJoCo env | runtime-confirmed for one-step playback of a short-chain live-dataset distill checkpoint only; no viewer window, trained policy quality, replay/storage, formal long training, or real teacher quality claim |
| `uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --task g1_stand_still/mujoco --steps 2 --action-mode policy --load-run <stand_student.pt> --device cpu` | S4 | T-live, T-connect, T-shape, T-persist | U-C-01, U-X-01, U-A-01, G1LOC-STAND-003 stand-still distill live playback; confirms the 98-D student checkpoint produced by bounded stand-still saved-dataset update loads under the explicit `g1_stand_still/mujoco` owner route, receives actor obs, emits finite nonzero 29-D policy actions, and steps the real `G1StandStill` MuJoCo env | runtime-confirmed for short-chain trained-student reset/step connectivity only; no viewer window, standing quality, formal long training, or MoE role-quality claim |
| `uv run scripts/train_distill.py ... training.offline_dataset_path=<saved_dataset.pt> training.offline_checkpoint=<student.pt>` | S2, S3 | T-connect, T-shape, T-persist, T-grad | U-E-02, U-A-01 generic distill saved-dataset offline update; confirms Hydra main loads a saved default 99-D/99-D distillation dataset, runs bounded MoE updates, prints finite loss/aux/usage diagnostics, saves a student checkpoint, and reloads through student-only playback with `checkpoint_actor_input_dim=99` metadata | runtime-confirmed for saved tensor dataset only; no replay collector, live dataset generation, formal long training, or trained policy quality claim |
| `uv run scripts/train_distill.py training.collect_dataset_path=<dataset.pt> training.collect_num_samples=1 training.collect_num_envs=1` | S3, S4 | T-live, T-connect, T-shape, T-persist | U-E-02, U-A-01 generic distill live-env dataset collection; confirms real `G1WalkHeight` MuJoCo reset produces a saved default `DistillationTensorDataset` with 99-D student obs and 99-D teacher obs via `teacher_projection=identity`, then reloads through dataset dim guards; explicit legacy `teacher.obs_dim=100 training.collect_teacher_projection=pad_zeros` still produces 100-D synthetic teacher tail | runtime-confirmed for reset/default zero-action collection and persistence only; no replay storage, teacher-policy action sampling, formal training, or trained policy quality claim |
| `uv run scripts/train_distill.py training.collect_dataset_path=<dataset.pt> training.collect_num_samples=3 training.collect_num_envs=1 training.collect_action_mode=random training.collect_action_seed=11` | S3, S4 | T-live, T-connect, T-shape, T-persist | U-E-02, U-A-01 generic distill live-env dataset collection random action source; confirms real `G1WalkHeight` MuJoCo `env.step` receives finite nonzero random 29-D actions, saves 99-D/99-D dataset metadata with `action_abs_max>0`, `env_steps>0`, `teacher_projection=identity`, and reloads through dataset dim guards | runtime-confirmed for nonzero random-action collection connectivity only; no teacher-policy action sampling, replay storage, formal training, teacher quality, or trained policy quality claim |
| `uv run scripts/train_distill.py task=g1_stand_still/mujoco training.collect_dataset_path=<dataset.pt> training.collect_num_samples=3 training.collect_num_envs=1 training.collect_action_mode=random training.collect_action_seed=11` | S3, S4 | T-live, T-connect, T-shape, T-persist | U-E-02, U-A-01, G1LOC-STAND-003 stand-still distill live-env dataset collection; confirms real `G1StandStill` MuJoCo `env.step` receives finite nonzero random 29-D actions, saves 98-D/98-D dataset metadata with `action_abs_max>0`, `env_steps>0`, `teacher_projection=identity`, and no synthetic tail | runtime-confirmed for explicit standing owner collection connectivity only; no teacher-policy action sampling, replay storage, formal training, or trained student quality claim |
| `uv run scripts/train_distill.py task=g1_stand_still/mujoco training.collect_dataset_path=<dataset.pt> training.collect_num_samples=3 training.collect_num_envs=1 training.collect_action_mode=teacher_policy` | S3, S4 | T-live, T-connect, T-shape, T-persist | U-E-02, U-A-01, G1LOC-STAND-003 stand-still teacher-policy live-env dataset collection; confirms real `G1StandStill` MuJoCo `env.step` receives finite teacher-policy 29-D actions from the accepted 98-D SAC standing teacher, saves 98-D/98-D dataset metadata with `teacher_policy_checkpoint_path`, `action_abs_max>0`, `env_steps>0`, `teacher_projection=identity`, and no synthetic tail; default `g1_walk_height` route rejects this mode | runtime-confirmed for explicit standing owner teacher-policy collection only; no replay storage, formal long training, height-conditioned walking teacher claim, or trained student quality claim |
| `uv run scripts/train_distill.py task=g1_stand_still/mujoco training.offline_dataset_path=<teacher_policy_dataset.pt> training.offline_checkpoint=<student.pt> training.offline_max_updates=1` | S2, S3 | T-connect, T-shape, T-persist, T-grad | U-E-02, U-A-01, G1LOC-STAND-003 stand-still saved-dataset offline update; confirms the 98-D teacher-policy live dataset feeds the frozen 98-D standing teacher, detached 29-D teacher action target, student update, and reloadable 98-D student checkpoint metadata | runtime-confirmed for bounded saved-dataset update only; no formal training loop, trained policy quality, or viewer/live student playback claim |
| `uv run scripts/train_distill.py task=g1_stand_still/mujoco training.formal_run=true training.formal_run_dir=<run_dir> training.offline_dataset_path=<teacher_policy_dataset.pt> training.offline_max_updates=1` | S2, S3 | T-connect, T-shape, T-persist, T-grad | U-E-02, U-A-01, G1LOC-STAND-003 stand-still formal saved-dataset distill lifecycle; confirms the explicit formal branch writes `run_config.json`, `run_summary.json`, and `model_<samples_seen>.pt`, reloads a 98-D student checkpoint, records completed summary metadata, and the formal checkpoint can be passed to the stand-still playback sentinel | runtime-confirmed for repeatable short saved-dataset lifecycle only; no replay/online training loop, viewer window, standing quality, or MoE role-quality claim |
| `uv run scripts/deploy/check_unilab_g1_distill_viewer_path.py --task g1_stand_still/mujoco --load-run <formal_run_dir> --checkpoint model_2.pt --action-mode policy --device cpu` plus `mjpython scripts/play_interactive.py --algo distill --task g1_stand_still --sim mujoco algo.load_run=<formal_run_dir> algo.checkpoint=model_2.pt interactive.action_mode=policy training.device=cpu` | S4 | T-live, T-connect, T-shape, T-persist | U-X-01, U-A-01, G1LOC-STAND-003 stand-still distill viewer path; confirms the formal 98-D student checkpoint enters the `play_interactive.py --algo distill` config/build route, creates a real `G1StandStill` MuJoCo playback session, emits finite nonzero 29-D policy actions, loads the viewer MuJoCo model, transfers physics state into viewer data, and starts the actual `mjpython` GUI path with the process alive after a 20-second hold probe | runtime-confirmed for GUI launch-path reachability; stdout opening/control-log capture, visual quality, standing quality, long-horizon behavior, and MoE role-quality remain unconfirmed |
| `uv run scripts/deploy/check_unilab_g1_distill_teacher_obs_contract.py --task <distill_task_owner> --checkpoint-path <teacher.pt> --device cpu` | S3, S4 | T-live, T-shape, T-persist | U-E-02, U-A-01, G1LOC-STAND-003 generic distill teacher obs audit; confirms current default `g1_walk_height/mujoco` has teacher/student 99-D and live `G1WalkHeight` obs 99-D/critic 102-D, and explicit `g1_stand_still/mujoco` has teacher/student 98-D, live `G1StandStill` obs 98-D/critic 101-D, and the accepted standing SAC teacher checkpoint actor input dim 98 | runtime-confirmed for live reset obs layout and config/checkpoint contract audit only; real trained teacher quality and height-conditioned walking teacher quality remain unconfirmed |
| `uv run pytest tests/scripts/test_start_sh.py -q` | S0, S2 | T-connect, T-oracle | G1LOC-E-001; confirms `start.sh` walking default, explicit/shorthand `g1_stand_still`, checkpoint shortcut, optional `--log-file` tee capture, keyboard default, and extra Hydra override preservation without launching viewer | contract-confirmed |
| `uv run pytest tests/config/test_reward_injection.py -q` | S0, S2 | T-connect, T-oracle, T-diff | U-C-02, U-R-02, G1LOC-C-001, G1LOC-C-002, G1LOC-C-003, G1LOC-C-004; includes upstream posture-anchor equality, G1StandStill high-support bundle ownership, `stand_base_height_deficit_l1` route, and G1WalkFlat/G1WalkHeight isolation from Standing high-support keys | contract-confirmed |
| `uv run pytest tests/envs/locomotion/g1/test_gait_constraint.py -q` | S1, S2 | T-value, T-role, T-oracle, T-meta, T-diff, T-mask, T-transform, T-scale | G1LOC-CMD-002, G1LOC-GAIT-001, G1LOC-RWD-001, G1LOC-OBS-001, G1LOC-RWD-002, G1LOC-GAIT-002, G1LOC-GAIT-003, G1LOC-STAND-001, G1LOC-STAND-002, G1LOC-ACT-001; includes stand-still pose/support reward lab ranking, high-support low-crouch ordering, and 0.677m/0.709m trained low-equilibrium regressions | contract-confirmed |
| `uv run pytest tests/envs/locomotion/g1/test_g1_height_tracking_contract.py -q` | S1, S2, S3 | T-shape, T-order, T-connect, T-persist, T-value, T-meta, T-scale, T-oracle | G1LOC-C-003, G1LOC-OBS-001, G1LOC-RWD-002 | contract-confirmed |
| `uv run pytest tests/envs/locomotion/g1/test_symmetry_contract.py -q` | S1, S2, S3 | T-shape, T-order, T-transform, T-persist | G1LOC-C-004, G1LOC-OBS-001, G1LOC-T-001 | contract-confirmed |
| `uv run scripts/deploy/check_unilab_g1_stand_still_live_sentinel.py --num-envs 16 --steps 64 --seed 11 --probe-mode anchor-search --anchor-search-candidates 48 --anchor-search-iterations 4 --anchor-search-elites 8` | S4 | T-live, T-oracle, T-value, T-role, T-scale, T-diff | G1LOC-C-004, G1LOC-STAND-001, G1LOC-ACT-001 | runtime-confirmed differential: zero-action fails stability gates, nonzero static anchor exists; trained-policy gap remains |
| `uv run pytest tests/visualization/test_interactive_playback.py -q` | S2, S3 | T-connect, T-persist, T-diff | U-X-01, G1LOC-P-003; includes HORA/SAC playback contracts and generic distill student playback session fake-env adapter | persistence-confirmed; no real viewer/live MuJoCo claim |
| `uv run pytest tests/base/test_registry.py tests/envs/test_env_configs.py -q` | S0, S2 | T-connect, T-oracle | U-R-01, G1LOC-REG-001 | contract-confirmed |
| `uv run pytest tests/base/test_np_env.py tests/utils/test_obs_utils.py tests/utils/test_final_observation.py -q` | S1, S2 | T-shape, T-order, T-mask, T-transform | U-O-01 | contract-confirmed |
| `uv run pytest tests/base/test_sim_backend.py tests/base/test_sim_backend_smoke.py -q` | S1, S2 | T-connect, T-oracle, T-shape | U-B-01 | contract-confirmed |
| `uv run pytest tests/base/test_mujoco_batch_env_jacobian.py tests/base/test_mujoco_batch_env_randomization.py tests/base/test_backend_pre_step_control.py -q` | S1, S2 | T-shape, T-connect, T-oracle | U-B-02 | contract-confirmed |
| `uv run pytest tests/base/test_motrix_backend_options.py tests/base/test_backend_imports.py -q` | S0, S2 | T-connect, T-oracle | U-B-03 | contract-confirmed |
| `uv run pytest tests/algos -q` | S1, S2, S3 | T-mask, T-value, T-grad, T-connect, T-detach, T-state | U-A-01, U-A-02 | contract-confirmed |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q` | S1, S3 | T-grad, T-detach, T-shape, T-persist | U-A-01 generic behavior distillation; proves fake teacher target detach, student-only gradient/update, student checkpoint roundtrip, SAC teacher checkpoint load, SAC teacher checkpoint actor input-dim inspection, dataset save/load roundtrip and dim guards, live-env collector projection guards, zero/random/teacher_policy/student_policy action-source contract, student-policy done-row reset guard and metadata, bounded offline update loop, offline behavior/aux diagnostics propagation, MLP/MoE student-only playback load, unknown student model-type rejection, MoE student soft/hard routing, MoE trainer aux loss, MoE router/expert gradients, and dim-mismatch guard | contract-confirmed; live temp-checkpoint route is covered by live sentinel, saved-dataset and live-collection entrypoints are covered by script tests/CLI probes, while replay storage, formal distillation loop, trained MoE policy quality, and real viewer playback remain gaps |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "distillation_dataset"` | S1, S3 | T-shape, T-persist, T-role | U-A-01 generic behavior distillation dataset contract; confirms old unlabeled datasets keep `role_labels=None`, role-labelled datasets preserve `DistillationTensorDataset.role_labels`, metadata `role_labels`, payload roundtrip, and `DistillationBatch.role_labels` slicing; bad length and empty labels fail explicitly | contract-confirmed for offline dataset semantics only; no live collection, multi-teacher sampling, MoE training quality, or policy quality claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "cached_teacher_actions or offline_distillation_run_accepts_cached"` | S1, S3 | T-grad, T-detach, T-shape, T-persist | U-A-01 generic behavior distillation cached-target contract; confirms `DistillationTensorDataset.teacher_actions` validates, persists, reloads, and slices cached 29-D teacher action targets; confirms `BehaviorDistillationTrainer` and `run_offline_distillation_updates` can update from cached targets without calling the teacher module and report `teacher_action_source=cached` | contract-confirmed for offline cached-target trainer plumbing only; no multi-teacher collection, replay storage, live training, trained MoE policy quality, or role-rich expert separation claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "role_conditioned_router_loss"` | S1, S3 | T-grad, T-role, T-shape | U-A-01 generic MoE role-conditioned router loss; confirms `role_loss_coef>0` with explicit `role_expert_targets` consumes `DistillationBatch.role_labels`, applies cross-entropy to MoE router logits, produces nonzero router gradients, records `role_loss` and `role_target_count`, and fails closed for missing mappings, missing labels, unmapped labels, or non-MoE students | contract-confirmed for offline role-conditioned loss only; no role-rich live collection, trained policy quality, or real expert specialization claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "command_intent_router_loss"` | S1, S3 | T-grad, T-role, T-shape | U-A-01 command-intent MoE router loss; confirms `command_intent_loss_coef>0` with explicit `command_intent_expert_targets` consumes `DistillationBatch.command_intents`, applies cross-entropy to MoE router logits, produces nonzero router gradients, records `command_intent_loss` and `command_intent_target_count`, and fails closed for missing mappings, missing labels, unmapped labels, or non-MoE students | implemented for CR-4; no deploy-time hard/bias route or live MuJoCo policy behavior claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "balanced_sampler or balances_role_batches or balances_command_intent"` | S1, S2, S3 | T-dist, T-role, T-shape, T-grad, T-persist | U-A-01 command-intent/role balanced offline sampler; confirms imbalanced source datasets can draw per-update balanced role batches and command-intent batches with replacement, records `batch_label_counts`, updates gradients from cached teacher actions, and fails closed when requested labels are absent | implemented for CR-6; proves offline sampler distribution, not live policy quality |
| `uv run pytest tests/visualization/test_interactive_playback.py -q -k "distill"` | S1, S2, S4 | T-connect, T-role, T-diff | U-X-01/U-A-01 generic distill playback command routing; confirms command intent thresholds, `auto` MoE hard routing for command-intent trained checkpoints, inactive commands selecting the stand expert, active commands selecting the walk expert, and old MLP playback still loading without routing | implemented for CR-5; proves playback contract and fake deployment action selection, not trained policy quality |
| `uv run pytest tests/scripts/test_train_scripts.py -q -k "role_conditioned_moe_trainer"` | S2, S3 | T-connect, T-persist, T-role | U-E-02, U-A-01 generic distill role-loss config connector; confirms Hydra owner fields `algo.role_loss_coef` and `+algo.role_expert_targets={...}` reach `BehaviorDistillationTrainer` and `distill_runtime_cfg` | contract-confirmed for script/config connector only; no simulator, training quality, or role-rich dataset claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "multitask_distillation_dataset_adapter"` | S1, S3 | T-shape, T-role, T-persist | U-A-01 generic distill multi-task dataset adapter; confirms multiple saved role-specific `DistillationTensorDataset` files with cached teacher actions merge into one role-labelled cached-target dataset, preserve source metadata, save/load through dim guards, infer a common source dim when no expected dim is supplied, and fail closed for empty sources, missing roles, missing cached targets, explicit expected-dim mismatch, or inconsistent source dims | contract-confirmed for saved-source assembly only; no live multi-task collection, teacher quality, trained MoE quality, or cross-owner projection claim |
| `uv run pytest tests/scripts/test_train_scripts.py -q -k "builds_multitask_dataset_from_saved_sources or multitask_dataset_infers_source_dims"` | S2, S3 | T-connect, T-shape, T-role, T-persist | U-E-02, U-A-01 generic distill multi-task adapter entrypoint; confirms `training.multitask_dataset_path` and `+training.multitask_sources=[{path,role},...]` route through `scripts/train_distill.py`, save a merged dataset, infer 98-D source dims instead of inheriting the default 99-D height config, and can be reloaded with role labels and cached teacher actions; optional `training.multitask_expected_*_dim` fields still enforce explicit expected dims | contract-confirmed for script connector only; no simulator/live collection/training-quality claim |
| `uv run pytest tests/scripts/test_train_scripts.py -q -k "offline_update_uses_balanced_role_sampler"` | S2, S3 | T-connect, T-dist, T-role, T-persist | U-E-02/U-A-01 saved-dataset offline distill balanced sampler connector; confirms `training.offline_balance_key=role` and `training.offline_balanced_labels=[stand,walk]` reach `run_offline_dataset_update`, produce balanced per-update label counts, save a checkpoint, and persist the balance settings in `distill_runtime_cfg` | implemented for CR-6; no live MuJoCo/trained-policy claim |
| `uv run scripts/deploy/check_unilab_g1_distill_multitask_runtime_probe.py --work-dir <tmp>` | S2, S3 | T-connect, T-grad, T-shape, T-role, T-persist | U-E-02, U-A-01 generic distill multi-task MoE runtime; confirms saved source datasets for `stand`, `walk_height`, and `height` merge into a six-sample role-labelled cached-target dataset, reload through dim guards, run two offline MoE updates, use `teacher_action_source=cached` without calling the raising teacher sentinel, and report positive role loss plus student grad norm | runtime-confirmed for toy tensor multi-task MoE connector only; no MuJoCo, live multi-task collection, real teacher quality, cross-owner projection, or trained policy behavior claim |
| `uv run scripts/deploy/check_unilab_g1_distill_dual_teacher_moe_probe.py --work-dir <tmp> --num-samples 4 --num-envs 1 --batch-size 4 --max-updates 2 --device cpu` | S2, S3, S4 | T-live, T-connect, T-grad, T-shape, T-role, T-dist, T-persist | U-E-02, U-A-01, G1LOC-STAND-003, G1LOC-CMD-002 real walking/standing dual-teacher MoE source path; confirms the provided `G1WalkFlat` and `G1StandStill` SAC checkpoints are 98-D, each explicit owner route can collect finite nonzero teacher-policy actions into cached 29-D `teacher_actions`, the two saved sources merge into one role-labelled dataset with `walk_flat` and `stand`, command intents persist as active/inactive, and a bounded MoE offline update uses cached targets plus balanced `walk_flat`/`stand` batches | runtime-confirmed for CR-7 short live source collection plus balanced offline connector; observed `last_counts={'walk_flat': 2, 'stand': 2}` with positive role and command-intent losses; no height source, no long training, no viewer, and no final fused-policy quality claim |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k moe` | S1 | T-grad, T-shape, T-role, T-oracle | U-A-01 generic MoE student/trainer/playback/diagnostics; proves local `MoEStudentPolicy` route logits/probs, expert action mixing, hard selected expert counts, bad-contract rejection, trainer aux loss, route entropy, expert usage stats, router/expert gradient flow, MoE student-only checkpoint playback reconstruction, toy role-wise expert diagnostics, collapse guard, and role-label mismatch rejection | contract-confirmed for local module, trainer, checkpoint playback loader, and diagnostic semantics; config selection and fake-batch entrypoint are covered in config/script tests, temp-checkpoint live route is covered by live sentinel, while trained MoE policy quality and real role-rich expert separation remain gaps |
| `uv run scripts/deploy/check_unilab_g1_distill_moe_expert_semantics.py --task g1_stand_still/mujoco --dataset-path <teacher_policy_dataset.pt> --student-checkpoint <moe_student.pt> --device cpu` | S3, S4 | T-live, T-role, T-shape, T-persist | U-A-01, G1LOC-STAND-003 stand-still MoE expert diagnostics; confirms a saved MoE student checkpoint and collected 98-D/98-D standing dataset reload, reports expert usage fractions, mean route entropy, dominant expert, and collapse guard; warns when dataset lacks role labels | runtime-confirmed for overall usage/entropy/collapse on the standing-only dataset; real command/height/velocity role separation remains unconfirmed because current dataset has no role labels or role diversity |
| `uv run pytest tests/ipc -q` | S1, S2, S3 | T-shape, T-order, T-mask, T-connect, T-persist | U-I-01, U-I-02 | connectivity-confirmed |
| `uv run pytest tests/training tests/utils/test_experiment_tracking.py -q` | S0, S2, S3 | T-connect, T-oracle, T-persist, T-diff | U-S-01 | persistence-confirmed |
| `uv run pytest tests/assets tests/test_export_scene.py tests/test_import_robot.py -q` | S0, S2, S3 | T-connect, T-persist, T-diff | U-X-01 | contract-confirmed |
| `uv run python -c "import json; from pathlib import Path; [json.loads(p.read_text()) for p in Path('note/architecture').glob('**/*.data.json')]"` | S0 | T-connect, T-oracle | U-V-01, G1LOC-V-001 | static-confirmed |
| `uv run ruff check <changed files>` | S0 | T-oracle | changed Python modules | static-confirmed |
| `uv run python -m py_compile <changed Python files>` | S0 | T-oracle | changed Python modules | static-confirmed |

## Proposed Live Sentinels

| Proposed command | S tiers | T kinds | Main modules | Status |
| --- | --- | --- | --- | --- |
| `uv run pytest tests/algos/test_g1_distillation_contract.py -q -k "command_intent or command_sample_filter or collect_distillation_dataset_from_env_filters or multitask_distillation_dataset_adapter_merges or balanced_sampler"` | S1, S3 | T-mask, T-role, T-dist, T-persist, T-grad | U-A-01 command-intent dataset, collector, router-loss, and offline balanced-sampler contract | implemented for CR-2/CR-4/CR-6; live trained-policy quality remains unconfirmed |
| `uv run pytest tests/scripts/test_train_scripts.py -q -k "owner_command_filter_override or collects_stand_still_dataset_with_owner_config or collects_owner_filtered_walk_dataset or collects_stand_still_teacher_policy_dataset_and_updates or collects_walk_flat_teacher_policy_cached_dataset or walk_flat_inactive_student_rollout"` | S2, S3 | T-connect, T-role, T-persist | U-E-02/U-A-01 config, collection, and multi-task assembly connector | implemented for CR-3/DAgger collection: includes G1WalkFlat+G1StandStill inactive student-policy rollout command-distribution override; router prior remains CR-4 |
| `UNILAB_G1_ACTION_TRACE=1 uv run scripts/deploy/check_unilab_g1_distill_playback_live_sentinel.py --task g1_walk_flat/mujoco --steps 40 --action-mode policy --load-run <moe_student.pt> --device cpu` | S4 | T-live, T-role, T-diff, T-scale | U-X-01/U-A-01 command-intent deploy routing | CR-5 playback/sentinel plumbing implemented and unit-tested; real trained-checkpoint live run still needs user-side MuJoCo execution |
| `uv run scripts/deploy/check_unilab_g1_policy_live_sentinel.py --load-run <run> --checkpoint model_5000.pt --steps 16` | S4 | T-live, T-oracle, T-scale, T-role | G1LOC-CMD-002, G1LOC-ENV-001, G1LOC-RWD-002, G1LOC-ACT-001 | missing |
| `uv run scripts/deploy/check_unilab_g1_standing_mode_dynamics.py --num-envs 16 --steps 16` | S4 | T-live, T-diff, T-oracle | G1LOC-STAND-001, G1LOC-ACT-001 | existing mixed-stage script, not the standalone G1StandStill owner route |
| `uv run scripts/deploy/check_unilab_g1_stand_still_live_sentinel.py --num-envs 8 --steps 128 --probe-mode support-policy-diff` | S4 | T-live, T-diff, T-scale, T-oracle | G1LOC-STAND-003, G1LOC-RWD-002, G1LOC-ACT-001 | executed: 64-step role summary confirms short-horizon high-support feasibility (`searched_support_action` height min 0.7219, tilt max 11.49 deg, both feet contact 1.0); 128-step run exposes long-horizon low-height gap at step 65 |

## Proposed Reward Lab Tests

| Proposed test | S tiers | T kinds | Main modules | Status |
| --- | --- | --- | --- | --- |
| `test_g1_stand_still_high_support_bundle_prefers_loaded_high_stand_over_low_crouch` | S1, S2 | T-value, T-role, T-diff, T-oracle | G1LOC-STAND-002, G1LOC-RWD-002 | implemented and passing after `stand_support_height_margin_l2`; `clean=0.280000`, `loaded_low=0.098012`, ratio `0.350` |
| `test_g1_stand_still_high_support_bundle_rejects_trained_low_equilibrium` | S1, S2 | T-value, T-role, T-diff, T-scale, T-oracle | G1LOC-STAND-002, G1LOC-RWD-002 | implemented after `log.txt` showed trained policy at `base_height≈0.677`; guards against this low equilibrium scoring above 50% of clean high support |
| `test_g1_stand_still_height_bundle_rejects_trained_0709_equilibrium` | S1, S2 | T-value, T-role, T-diff, T-scale, T-oracle | G1LOC-STAND-002, G1LOC-RWD-002 | implemented after playback improved to `base_height≈0.709` but remained low; `stand_base_height_deficit_l1=-240.0` drives the 0.709 row from 0.878x clean to 0.450x clean |

## Inventory Rule

When a new test is added, update this file with:

```text
command -> S tiers -> T kinds -> modules -> evidence class -> remaining gap
```
