# @package _global_
defaults:
  - /task/sac/g1_walk_flat/mujoco_fada_source
  - _self_

# D4 ablation: phase observation + fixed-frequency contact scheduling only.
algo:
  actor:
    oracle_behavior_profile: fixed_phase_contact_v1

env:
  gait_phase_enabled: true
  gait_phase_init_mode: offset_phase
  commands:
    dead_zone_enabled: false
  command_gated_phase_contact:
    enabled: false
    # Only these static contact parameters are consumed; no command state machine.
    duty_factor: 0.55
    contact_force_threshold: 1.0

reward:
  feet_phase_mode: fixed_phase_contact_v1
  gait_frequency: 1.5
  scales:
    phase_contact: 1.0
    null_foot_force_balance: 0.0
    null_torque_relaxation: 0.0
