# @package _global_
defaults:
  - /task/sac/g1_walk_flat/mujoco_fada_privileged_oracle_original_height_grouped_dr_lineage
  - _self_

# Stage 1: tighter linear tracking and reduced action-change cost only.
reward:
  tracking_lin_vel_sigma: 0.04
  tracking_ang_vel_sigma: 0.25
  scales:
    penalty_action_rate: -1.0
