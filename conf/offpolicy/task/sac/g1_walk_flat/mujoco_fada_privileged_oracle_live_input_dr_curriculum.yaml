# @package _global_
defaults:
  - /task/sac/g1_walk_flat/mujoco_fada_privileged_oracle_live_input_nominal_5k
  - _self_

# Non-sealed policy-quality validation. Expand only the left-knee gain fault
# distribution after the current stage sustains long episodes.
algo:
  privileged_nominal_validation: false
  privileged_dr_curriculum_validation: true

env:
  domain_rand:
    actuator_strength:
      enabled: true
      curriculum_enabled: true
      curriculum_multiplier_lows: [1.0, 0.98, 0.95, 0.9, 0.85, 0.8]
      curriculum_nominal_probabilities: [1.0, 0.8, 0.7, 0.5, 0.4, 0.3]
      curriculum_promote_threshold: 800.0
      curriculum_demote_threshold: 500.0
      curriculum_update_episodes: 1024
